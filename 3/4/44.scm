;; Ben Bitdiddle is right. Transfer procedure is different form exchange
;; procedure in that it does not compute intermediate value (difference) and
;; having deposit/withdraw serialized is enough here.